---
import Layout from '../layouts/Layout.astro';
import Hero from '../components/Hero.astro';
import ProductGrid from '../components/ProductGrid.astro';

// mock categories
const categories = [
  { name: 'Sofás', image: '🛋️', slug: 'sofas' },
  { name: 'Camas', image: '🛏️', slug: 'camas' },
  { name: 'Mesas', image: '🪑', slug: 'mesas' },
  { name: 'Organización', image: '📦', slug: 'organizacion' },
  { name: 'Cocina', image: '🍳', slug: 'cocina' },
  { name: 'Decoración', image: '🖼️', slug: 'decoracion' },
  { name: 'Iluminación', image: '💡', slug: 'iluminacion' },
  { name: 'Textiles', image: '🧵', slug: 'textiles' },
];
---

<Layout title="NORDY. | Beautiful Minimal Design Home">
	<Hero />
  
  <section class="categories-section container">
    <div class="category-scroll">
      {categories.map(cat => (
        <a href={`/categorias/${cat.slug}`} class="category-item">
          <div class="category-icon">{cat.image}</div>
          <span class="category-name">{cat.name}</span>
        </a>
      ))}
    </div>
  </section>

  <ProductGrid />

  <!-- Promo banner like Casa Ideas / Ikea often uses -->
  <section class="promo-banner container">
    <div class="promo-content">
      <h2>Únete a la familia NORDY</h2>
      <p>Obtén descuentos exclusivos, acceso anticipado a nuevas colecciones e ideas inspiradoras para tu hogar.</p>
      <button class="btn btn-primary" aria-label="Unirse gratis">Unirse gratis</button>
    </div>
  </section>
  
  <ProductGrid title="Recién Llegados" subtitle="Nuevos diseños frescos para inspirarte." />
</Layout>

<style>
  .categories-section {
    padding-top: 2rem;
    padding-bottom: 2rem;
  }

  .category-scroll {
    display: flex;
    gap: 2rem;
    overflow-x: auto;
    padding-bottom: 1rem;
    scrollbar-width: none; /* Firefox */
  }

  .category-scroll::-webkit-scrollbar {
    display: none; /* Chrome/Safari */
  }

  .category-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.8rem;
    min-width: 80px;
    transition: transform var(--transition-fast);
  }

  .category-item:hover {
    transform: translateY(-5px);
  }

  .category-icon {
    width: 80px;
    height: 80px;
    border-radius: 50%;
    background-color: var(--color-surface);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 2rem;
    transition: background-color var(--transition-fast);
  }

  .category-item:hover .category-icon {
    background-color: var(--color-border);
  }

  .category-name {
    font-size: 0.9rem;
    font-weight: 600;
  }

  /* Promo Banner */
  .promo-banner {
    margin-top: 3rem;
    margin-bottom: 3rem;
  }

  .promo-content {
    background-color: #fce2c2; /* Warm inviting color, common in Casa ideas */
    padding: 4rem;
    border-radius: var(--radius-lg);
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.5rem;
  }

  .promo-content h2 {
    font-size: 2.5rem;
    color: #a45a1c;
    letter-spacing: -1px;
  }

  .promo-content p {
    font-size: 1.1rem;
    max-width: 600px;
    color: #a45a1c;
  }

  .promo-content .btn {
    background-color: #a45a1c;
    color: white;
    font-size: 1.1rem;
    padding: 1rem 2.5rem;
  }

  .promo-content .btn:hover {
    background-color: #7b4315;
  }

  @media (max-width: 768px) {
    .promo-content {
      padding: 2.5rem 1.5rem;
    }
    .promo-content h2 {
      font-size: 2rem;
    }
  }
</style>
